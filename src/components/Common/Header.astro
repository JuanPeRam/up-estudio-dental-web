---
interface Props {
  title: string;
  description: string;
  styles?: string;
}

const { title, description, styles } = Astro.props;
---

<header
  class={`${styles} min-h-[20vh] column-center w-full pt-20 pb-5 px-10 gap-5 *:max-w-[80ch] `}
>
  <h1 class="text-5xl text-center uppercase animate-slide-in-bottom">
    {title}
  </h1>
  <p class="text-pretty animate-slide-in-bottom">
    {description}
  </p>
</header>
