---
import CarrouselAstro from "@/components/Carrousel/CarrouselAstro.astro";
import Carrousel from "@/components/Carrousel/Carrousel.astro";
---

<section class="column-center p-5">
  <div id="carrousel" class="hidden p-5 backdrop-blur-md rounded-md">
    <Carrousel />
  </div>

  <div
    id="carrouselAstro"
    class="hidden rounded-md animate-blurred-fade-in w-full"
  >
    <CarrouselAstro />
  </div>
</section>

<script>
  function handleResize() {
    const carousel = document.getElementById("carrousel");
    const carouselAstro = document.getElementById("carrouselAstro");
    const width = window.innerWidth;
    if (carousel && carouselAstro) {
      if (width >= 750) {
        carousel.style.display = "none";
        carouselAstro.style.display = "block";
      } else {
        carousel.style.display = "block";
        carouselAstro.style.display = "none";
      }
    }
  }

  window.addEventListener("DOMContentLoaded", handleResize);
  window.addEventListener("resize", handleResize);
  document.addEventListener("astro:after-swap", handleResize);

  handleResize();
</script>
